import { TestBed, async } from '@angular/core/testing';
import { DummyCodeComponent } from './dummyCode.component';

describe('DummyCodeComponent', () => {
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [
        DummyCodeComponent
      ],
    }).compileComponents();
  }));
});
